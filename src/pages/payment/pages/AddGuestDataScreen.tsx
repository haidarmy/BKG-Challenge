import React from 'react';
import {Screen} from '@components';
import AddGuestData from '../containers/AddGuestData';

const AddGuestDataScreen = () => {
  return (
    <Screen>
      <AddGuestData />
    </Screen>
  );
};

export default AddGuestDataScreen;
